<%= form_for(subject_area) do |f| %>
  <% if subject_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subject_area.errors.count, "error") %> prohibited this subject_area from being saved:</h2>

      <ul>
      <% subject_area.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.hidden_field :uni_id, :value => "#{params[:uni_id]}" %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
