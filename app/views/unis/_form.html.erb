<%= form_for uni, html: {multipart: true} do |f| %>
  <% if uni.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(uni.errors.count, "error") %> prohibited this uni from being saved:</h2>

      <ul>
      <% uni.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :avatar %>
    <%= f.file_field :avatar %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
